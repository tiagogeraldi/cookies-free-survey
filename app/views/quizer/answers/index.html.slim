| Answers

p = @quiz.description
p = @session_hex

- if @question
  = link_to 'Start Survey',
    new_quizer_answer_path(s: @quiz.audience_secret, question_id: @question.id, session_hex: @session_hex),
    class: 'btn btn-lg btn-primary'
