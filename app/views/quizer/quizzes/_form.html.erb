<%= form_with(model: quizer_quiz) do |form| %>
  <% if quizer_quiz.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quizer_quiz.errors.count, "error") %> prohibited this quizer_quiz from being saved:</h2>

      <ul>
        <% quizer_quiz.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :public_results, style: "display: block" %>
    <%= form.check_box :public_results %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
