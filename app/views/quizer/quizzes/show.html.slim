.btn-group.mb-2
  - if @quizer_quiz.questions.count == 0
    = link_to 'Add a question', new_quizer_quiz_question_path(@quizer_quiz),
      class: 'btn btn-primary'
  - else
    = link_to quizer_quiz_questions_url(@quizer_quiz), class: 'btn btn-primary' do
      | Quiz questions
      span.badge.text-bg-secondary = @quizer_quiz.questions.count

  = link_to "Edit description", edit_quizer_quiz_path(@quizer_quiz), class: 'btn btn-secondary'

  = link_to "See the results", results_quizer_quizzes_path(s: @quizer_quiz.audience_secret), class: 'btn btn-success'

.row
  .col-6
    .card
      .card-body
        p Save this link to return to this page and edit the quiz or go to the results:
        .input-group.mb-3
          = text_field_tag 'owner-link', url_for([@quizer_quiz, { only_path: false }]), readonly: true, class: 'form-control'
          span.input-group-text
            = link_to 'Copy', '#', class: 'clipboard', data: { clipboard_target: '#owner-link' }
  .col-6
    .card
      .card-body
        p When finished creating your questions, send this link to your audience:
        .input-group.mb-3
          = text_field_tag 'audience-link', quizer_quizzes_url(s: @quizer_quiz.audience_secret,
            only_path: false), readonly: true, class: 'form-control'
          span.input-group-text
            = link_to 'Copy', '#', class: 'clipboard', data: { clipboard_target: '#audience-link' }

= render @quizer_quiz

p
  = button_to "Delete quiz", @quizer_quiz,
    method: :delete,
    class: 'btn btn-danger btn-sm',
    data: { turbo_confirm: 'Are you sure you want to delete this quiz?' }
